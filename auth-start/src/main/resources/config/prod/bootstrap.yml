spring:
  application:
    name: auth
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        group: DEFAULT_GROUP
        fileExtension: yaml
        shared-configs[0]:
          data-id: application.yaml
          group: DEFAULT_GROUP
          refresh: true
